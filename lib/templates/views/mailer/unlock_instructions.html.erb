<p><%= t(:'devise.mailer.unlock.title', :email => @resource.email) %></p>

<p><%= t(:'devise.mailer.unlock.description') %></p>

<p><%= link_to t(:'devise.mailer.unlock.link'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
